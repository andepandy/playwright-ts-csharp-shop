{
  "name": "playwright-shop-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Fluent Playwright API tests and Playwright C# UI tests for the same shop journeys",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "author": "GitYourCodeOn",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
